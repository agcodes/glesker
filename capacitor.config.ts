import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.glesker.app',
  appName: 'glesker-app',
  webDir: 'dist/glesker-app/browser',
};

export default config;
